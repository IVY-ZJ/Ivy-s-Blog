# 2026-08-11 工作日志

## 博客登录页（login.html）搭建完成

### 新增/修改文件
- **新增 `login.html`**：全屏照片背景（`assets/img/login-bg.jpg`，来自根目录原 `e07ab5c4...jpg`；暗色模式自动切 `login-bg-dark.webp`），玻璃拟态登录卡片，左侧品牌、右下主题切换、右侧竖排诗句。
- **新增 `assets/css/login.css`**：登录页专属样式（玻璃卡片、诗句竖排 `writing-mode: vertical-rl`、验证轨道、toast）。注意：必须用 `.login-body::before/::after { content: none !important }` 屏蔽主样式里的全局壁纸层，否则背景被覆盖（踩坑）。
- **新增 `assets/js/login.js`**：
  - 60 句积极立意诗句库（一句诗+作者，随机展示，`sessionStorage` 记录上一句避免连续重复，右下翻转按钮可切换）。
  - 拖动验证：6 种 SVG 图形（月亮/太阳/太极/雪花/星/叶）随机生成，拖动图形到右侧虚线轮廓（容差 14px），支持鼠标/触摸（pointer events）+ 键盘方向键，成功打勾解锁登录按钮。
  - 主题切换复用主站 `Ivy-theme` 键。
  - 登录态存 `localStorage['ivy-session']`（用户名+哈希+时间戳，30 天 TTL）；已登录访问 login.html 自动跳回首页。
  - 本地演示验证：任意用户名 + 密码≥4位即可通过（纯前端演示，非安全方案，注释已说明）。
- **修改 `assets/js/app.js`**：`ensureSessionNodes()` 自动向所有页面的 `.sidebar-foot` 注入登录胶囊（已登录显示用户名+退出，未登录显示登录按钮），`/posts/` 内页自动用 `../login.html` 相对路径。
- **修改 `assets/css/style.css`**：追加 `.session-pill` / `.login-cta` 样式（底部 1604 行之后）。
- **修改 `index.html` / `README.md`**：README 补充登录页说明与目录结构。

### 测试情况（agent-browser + 系统 Chrome）
- 拖动验证通过 → 登录按钮解锁 ✓
- 完整登录 → 跳转 index.html ✓，侧边栏显示登录胶囊 ✓，退出登录跳回 login.html ✓
- 诗句随机+翻转切换 ✓，主题亮/暗切换持久化 ✓，密码显隐 ✓
- 移动端 390px 视口：诗句变横排、卡片自适应 ✓
- 已登录访问 login.html 自动跳转 ✓

### 踩坑记录
- agent-browser 自带 Chrome 在 Windows 报 exit code 3，改用 `--executable-path "C:\Program Files\Google\Chrome\Application\chrome.exe"` 解决。
- 全局 `body::before` 壁纸层优先级高于 `.login-bg`（z-index -3 vs -2），需用 `content: none !important` 关闭。

## 第二轮迭代（晚间）

### 用户反馈三项改动
1. **暗色背景替换**：用户新加 `9baa6a6204bdb584837715f8522670ee191ee008.jpg`（星空+巨石阵），复制为 `assets/img/login-bg-dark.jpg`（替换原浅色 webp 副本），CSS 引用更新。
2. **去掉副标题**：「把学到的东西，认真地记下来。」整段删除，仅保留「欢迎回来」。
3. **密码换邮箱 + 诗句改上下两句**：
   - HTML 表单删 user/password 两个字段，新增 email 字段（信封图标）；"忘了密码？"改"遇到问题？"。
   - `POETRY` 数组从 `[句, 作者]` 改为 `[上句, 下句, 作者]`，全部 60 首都补全下句（替换掉原本单句的「人间有味是清欢」「此心安处是吾乡」等）；`renderPoetry()` 渲染 `#poetry-line-2`。
   - HTML 加 `<span class="poetry-line poetry-line-2">`；CSS 给 `.poetry-line-2 { margin-top: -2px }` 让两行紧凑。
   - sessionStorage key 从 `ivy-last-poem` 改为 `ivy-last-couplet`（更准确）。
   - 校验改为邮箱格式正则 `/^[^\s@]+@[^\s@]+\.[^\s@]+$/`，session 同时存 `email` 完整字段和 `username`（邮箱前缀作为显示名）。localStorage key `ivy-remember-username` → `ivy-remember-email`。

### 移动端诗句栏与品牌重叠修复
- 原因：原 `.login-poetry` 在桌面端是 `position: fixed` 居中在右侧，移动端改成 `position: static` 排到顶部后，跟同样 fixed 在左上角的 `.login-brand` 在水平位置重叠。
- 尝试：①给 poetry `margin-top: 64px` 让位 ❌（仍是水平重叠）；②把 brand z-index 调到 100 ❌（仍视觉重叠，因为 poetry 也横向铺满顶部）；③flex column + order ❌（破坏了 main 居中布局，login-card 反倒跑到顶部）。
- 最终方案：保持 brand `position: fixed`（top:16/left:16）+ 简化（小屏隐藏副标题 `em`、mark-disc 缩到 28px、strong 15px）；poetry 用 `position: static; margin: 64px auto 6px` 推下去；brand `z-index: 100` 兜底确保覆盖在 poetry 之上。

### 验证结果（agent-browser）
- 亮色：沙漠黄昏背景 + 玻璃卡 + 上下两句诗句 ✓
- 暗色：星空巨石背景 + 深色卡 + 上下两句诗句 ✓
- 邮箱错误格式拒绝（"请输入正确的邮箱地址"） ✓
- 邮箱正确 → 登录 → 跳首页 → 胶囊显示邮箱前缀 ✓
- 移动端 390px：brand 缩小 + poetry 横排两行 + card 适配 ✓

## 第三轮迭代（晚 7:30）

### 用户反馈五项改动
1. **左上角品牌去副标题、用头像代替绿球**：`mark-disc`（绿色渐变圆球）→ `mark-avatar`（`<img src="avatar.jpg">`），删除 `<em>学习笔记 · 资源分享</em>`。
2. **诗句拆两列**：HTML 结构改成 `.poetry-couplet`（含上下两句）+ `.poetry-author-col`（含 dash + 作者），`.poetry-rail` 仍是 `flex-direction: column`（竖排下 = 水平并排）。移动端 `horizontal-tb` 下覆写：rail 改 `row`，couplet / author-col 改 `column` 让两句诗上下堆叠。
3. **卡片居中、删 footer、加用户名**：
   - `.login-card` 改 `position: fixed; top: 50%; left: 50%; transform: translate(-50%, -50%)` 真正居中。
   - 删除 `card-foot`（"还没有账号？联系 Ivy 开通"整段）。
   - 新增 `<input type="text" name="username">` 字段（person 图标，placeholder "Ivy"），放在邮箱上方。
   - 校验逻辑：先查 username 非空，再查 email 格式正则，session 存用户输入的 username（不再用邮箱前缀）。
   - 移动端卡片位置微调 `top: 54%` 避免被顶部诗句栏挡住，加 `max-height` + `overflow-y: auto` 防止溢出。
   - `cardEnter` 关键帧改成包含 `translate(-50%, -50%)` 让初始动画也对齐。
4. **侧边栏显示 "Hello, XXX"**：
   - `app.js` 的 `ensureSessionNodes()` 在每个 `.brand` 元素后注入 `<div class="hello-line" data-hello-line hidden><span>Hello,</span> <b data-hello-name></b></div>`。
   - `renderSession()` 增加 hello 行显隐 + 用户名赋值。
   - `style.css` 新增 `.hello-line` 样式（浅灰字 + 用户名加粗 + pop 动画）。
5. **背景清晰化**：`.login-bg-tint` 去掉 `backdrop-filter: blur(2px)`，只保留轻微径向/线性渐变；渐变透明度从 `0.25→0.35` 调低到 `0.18→0.22`。背景图本身清晰可见。
6. **"欢迎回来"→"欢迎"**：直接改 `<h1>` 文案。

### 验证（agent-browser）
- 桌面亮色（1440×900）：品牌头像 + "Ivy"、背景图清晰、卡片居中、用户名+邮箱两字段、诗句"一点浩然气/千里快哉风 — 苏轼"两列展示（作者在左、诗句在右）✓
- 桌面暗色：星空巨石清晰、深色卡 ✓
- 移动端 390px：所有元素正常堆叠，诗词栏在卡片上方 ✓
- 登录 → 跳首页 → 侧边栏 brand 下方显示 "Hello, **Ivy**" ✓

## 第四轮迭代（晚 8 点）

### 用户反馈五项改动
1. **删登录卡头像 + 彩色环移到左上角**：
   - 删 `login.html` 的 `.card-head > .avatar-ring` 整块（连带删 `login.css` 的 `.avatar-ring` / `.avatar-ring img` / dark 覆写规则）。
   - `.mark-avatar` 由白边框改为彩色渐变环：去掉 `border: 2px solid rgba(255,255,255,0.85)`，改用 `padding: 2px; background: linear-gradient(135deg, #5ac8fa, #007aff 50%, #af52de); box-shadow: 0 6px 18px rgba(0,122,255,0.35)`。
   - 顺手把移动端媒体查询里残留的死规则 `.mark-disc` 改成 `.mark-avatar { width:30px; height:30px; }`（之前改名后忘了同步）。
2. **修复左上角直达 + 网站全局登录守卫**：
   - `login.html` 的 `.brand-mark` 从 `<a href="index.html">` 改为 `<div role="img" aria-label="Ivy">` —— 在 login 页本身点左上角不应跳站。
   - `app.js` IIFE 顶部加 `guardSession()`：检查 `ivy-session` 是否在 30 天 TTL 内，无则 `location.replace(loginPathFor() + "?back=" + ...)`，顶层页用 `login.html`，`/posts/` 用 `../login.html`。IIFE 在 redirect 后 `return`，避免后续 DOM 操作白做。`login.html` 不加载 `app.js`（只加载 `login.js`），无重定向循环。
3. **删诗句横杠**：`login.html` 删 `.poetry-dash` 元素，`login.css` 删对应样式块。
4. **删"遇到问题？"按钮**：`login.html` `.form-row` 删 `<a class="link-mute">`，`login.css` 删 `.link-mute` / `.link-mute:hover`。
5. **三项齐全才可登录 + 邮箱后缀审查**：
   - 新增 `isValidEmail()`：正则升级 `/^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}$/`（本地部分限定安全字符、域名要求点分段、TLD 必须 2+ 字母），符合 RFC 风格邮箱规范；取代旧的 `/^[^\s@]+@[^\s@]+\.[^\s@]+$/`（那个允许单字符后缀）。
   - 新增 `canLogin()` + `syncLoginBtn()`：登录按钮仅在「人机验证通过 ∧ 用户名非空 ∧ 邮箱通过 isValidEmail」时启用。`enableLogin()` 改为调 `syncLoginBtn()`，使人机验证成功后再校验两项输入。`#username` / `#email` 的 `input` 事件触发 `syncLoginBtn`，做到实时联动。
   - `initForm` 提交时给出更具体的错误提示："邮箱格式不正确，请检查后缀（如 you@example.com）"。

### 验证（agent-browser + 系统 Chrome，全过程）
- **结构断言**（eval）：`.brand-mark` tagName=DIV、href=null；`.avatar-ring`/`.poetry-dash`/`.link-mute` 全部不存在；`.mark-avatar` 计算样式 backgroundImage = `linear-gradient(135deg, rgb(90,200,250), rgb(0,122,255)...`；按钮初始 disabled=true。
- **登录门控**：填坏邮箱 → 按钮仍 disabled；模拟 pointer 拖过验证 → captcha verified=true 但按钮仍 disabled；改正邮箱 → 按钮 enabled；清空用户名 → 按钮又 disabled。✅
- **登录流**：填 Ivy / ivy@example.com → 拖过验证 → submit → URL 变为 `http://localhost:8080/index.html`，session 写入（username=Ivy, email=ivy@example.com），侧边栏 `Hello, Ivy` 显示。✅
- **守卫**：(a) 无 session 直接 `open index.html` → 跳 `login.html?back=index.html`；(b) `open posts/why-i-write.html` → 跳 `login.html?back=posts%2Fwhy-i-write.html`（相对路径正确）。✅
- **登出**：点登录胶囊退出按钮 → session 清空 → 自动回 `login.html`。✅
- **截图**：preview-r4/login-light-forced.png 与 login-dark-forced.png 已留档，可见顶部头像彩色环、卡头只剩"欢迎"、诗句列两列无横杠、底部无"遇到问题？"、登录按钮在三项未齐时为淡蓝禁用态。

### 踩坑
- agent-browser 截图路径必须用 Windows 形式 `C:/Users/...`（Git Bash 的 `/c/Users/...` 形式不被 Rust 端识别，报 "系统找不到指定的路径" os error 3）；eval/open 不涉及文件路径所以不受影响。
- taskkill 在 Git Bash 下因 MSYS arg 转换，`/F` 会被误当路径而失败；本地测试用 Python http.server 残留进程无碍。

### 同期
- 新建项目级长期记忆 `.workbuddy/memory/MEMORY.md`，提炼登录系统架构/决策/坑点，便于未来 session 一眼恢复上下文。

## 第五轮迭代（晚 8:40）

### 用户反馈四项改动
1. **品牌区去掉副标题、Ivy 垂直居中**：
   - 13 个 HTML（`about/index/music/notes/posts/resources/tags.html` + 5 个 `posts/*.html`）批量删 `<span class="brand-sub">学习笔记 · 资源分享</span>`；`posts/` 内页是单行形式 `<span class="brand-name">Ivy</span><span class="brand-sub">…</span>`，主站是多行带缩进，统一用一个 Python 脚本处理两种格式。
   - `.brand` CSS 加 `min-height: 48px`；`.brand-text` 加 `justify-content: center; min-height: 44px`；`.brand-name` 从 `17px / weight 700` 提到 `18px / weight 800`。实测 `getBoundingClientRect`：avatar center 与 brand-name center 完全重合（都 y=53）。
2. **Hello, xxx 移到右侧栏上方（与头像齐平或略高）**：
   - 不再注入到 `.brand` 之后，改在 `.right-rail` 存在的页面（仅首页）单独注入一个 `position: fixed` 的 `.rail-greeting` 到 body，top=`calc(env + 22px)` right=`16px` width=`220px`。
   - 内容：`欢迎回来` eyebrow + `Hello, {name} 👋`（带 wave emoji 摆动动画），实测 greeting center y=46 vs avatar center y=53，差 7px，即"略高于头像"。
   - tags / 音乐卡片位置不变（greeting 是 fixed 浮层，不影响右栏排版流）。
   - 没有 right-rail 的页面（about/posts/music 等）继续在 `.brand` 后注入旧 `.hello-line` 兜底，保持登录反馈。
   - 响应式：`@media (max-width: 1280px) { .rail-greeting { display: none } }`，实测 1200 视口时 `getComputedStyle.display === 'none'`。
3. **去掉"本地音频等"字眼**：
   - `app.js` `npLoad()`：`npArtistEl.textContent = ""`（之前是 `"本地音频"`），即选中曲目后播放器下方不再有副标题。
   - `music.html` 内联脚本 `play()`：`"本地音频 · " + 文件名` 改为 `"— " + 文件名`，实测 `mpSub.textContent === "— Eutopia.flac"`。
4. **侧栏宽度改 min 230 / max 260**：
   - `app.js` `MIN_W = 230, MAX_W = 260`（之前 220/270）。
   - CSS 默认 `--sidebar-w: 260px` 不变。
   - 实测：`localStorage['Ivy-sidebar-w']=220` → 加载后被 clamp 到 230；`localStorage=280` → clamp 到 260；删了之后回归 260。

### 验证（agent-browser + 系统 Chrome）
- 1440×900 亮色：brand 只有 avatar + Ivy，Ivy 与头像同高；右栏顶部「欢迎回来 / Hello, Ivy 👋」紧贴 tags 卡片上方，不挤压 tags 与音乐 ✓
- 1440×900 暗色：同样布局 ✓
- 1200 视口：right-rail 内的 greeting 自动隐藏（fallback 到右侧栏布局变更） ✓
- about/posts.html 等无右栏页：sidebar 在 brand 下方正常显示 "Hello, Ivy" ✓
- 侧栏拖宽（220 280 localStorage）→ 实际宽度均被夹到 230~260 ✓
- 音乐页点击曲目后：mpSub = `— Eutopia.flac`（无"本地音频"） ✓
- 首页 mini 播放器点曲目后：npArtist.textContent = `""` ✓

### 截图留档
- `preview-r5/final-home-light.png`、`final-home-dark.png`：新首页布局
- `preview-r5/04-music-light.png`、`05-about-light.png`、`06-posts-light.png`：其他页面品牌区/文本校对
- 历史四轮 `preview-r4/` 仍保留（Round 4 验收图）

### 踩坑
- agent-browser screenshot 子命令的参数顺序：`screenshot <path>`，不要加 `--viewport` 在子命令里——viewport 用 `set viewport <w> <h>`。第一次误把 `--viewport 1280x900` 加在 screenshot 后，输出变成 `Screenshot saved to --viewport`。
- 截图前必须有有效 session，否则 guard 直接跳到 `login.html`。用 init-script（`--init-script`）把预制 ivy-session 写到 localStorage，绕过登录门控又能继续走完整页面。
- 即便 `eval "localStorage.clear()"` 清掉，**init-script 会在每次新导航时重新写入**，导致 session 永远存在。要测试「未登录态」必须 `close --all` 重启 daemon，不要再带 `--init-script`。

## 第六轮迭代（晚 9 点）

### 用户反馈四项改动
1. **所有页面左侧栏不再有 Hello, xxx**：
   - `app.js` `ensureSessionNodes()` 删掉 `!hasRail` 时向 `.brand` 后注入 `.hello-line` 的整段逻辑。R5 兜底用的 hello-line 全部清掉。
   - 验证：`document.querySelector('[data-hello-line]')` 在 about/posts/music 等页 `=== null`；`.brand.nextElementSibling` 是 `<nav>`，不是 hello div。
2. **首页右栏问候语瘦身 + 上移**：
   - 去掉 `.rail-greeting` 里的「欢迎回来」eyebrow，只保留单行 `Hello, {name} 👋`；CSS 把对应 `.rg-eyebrow` 规则整段删除。
   - top 从 `env + 22` 改为 `env + 14`，right 16→18；高度实测 27.5px（无 eyebrow），greeting bottom = 41.5 < tags card top = 56，**不再压住** tags，留 14.5px 间距。
3. **登录验证图去掉太极/八卦**：
   - `login.js` 第 107-111 行的 3 号 SVG（yin-yang 黑白圆）整段替换成粉色心形（`fill="#ff2d55"`, `stroke="#ff3b30"`, 标准双圆顶尖 + 三角曲线路径），6 张图变：月/日/❤️ 雪花/星/叶。
   - 验证：`grep -c "circle cx=\"28\" cy=\"28\" r=\"20\"" login.js` → 0（旧的 yi-yang 大圆已无）；连续刷 10 次出现 5 种颜色（蓝紫/黄/粉/橙/绿），其中粉（心）出现 2 次。
4. **登录页用户名 placeholder 改通用**：
   - `login.html` `<input id="username">` 的 `placeholder="Ivy"` 改成 `placeholder="你的名字"`。

### 验证（agent-browser + 系统 Chrome，1440×900）
- 亮 + 暗首页：仅在右栏看到 `Hello, Ivy 👋` 单行；tags / 音乐卡片位置完全不变 ✓
- about / posts.html：sidebar 完全无 Hello，brand 后直接接 `<nav>` ✓
- login.html：用户名输入框显示「你的名字」；captcha 循环刷新采到 5 种不同色（蓝、黄、粉、橙、绿），无黑白阴阳图；20 次连刷未出现 yin-yang 旧路径 ✓

### 截图留档
- `preview-r6/01-home-light.png`、`04-home-dark.png`：新首页（无侧栏问候、单行右栏问候）
- `preview-r6/02-about-light.png`：about 页 sidebar 干净，无 Hello
- `preview-r6/03-login-light.png`：登录页新 placeholder + 新 captcha（随机切换）

### 收尾（晚 10:30，用户要求"继续完成未完成的任务"后）
- **清死代码**：`style.css` 删除已无注入的 `.hello-line` 规则块（309-320 行）与 `@keyframes helloPop`（367-370 行）。校验：`{`/`}` 平衡、`hello-line`/`helloPop` 全文件 0 引用、`.rail-greeting` 保留 6 处。
- **README 同步**：
  - 「可拖动侧边栏」宽度描述从 `220–420px` 改为 `230–260px`（与 R5 的 MIN_W/MAX_W 一致）。
  - 「登录页」特性补充：首页右栏顶部显示单行问候「Hello, 用户名」。
- **全站一致性确认**：`grep "Hello," *.html *.js` 只剩 `app.js:90` 一处 —— 即首页右栏 rail-greeting 的注入，无其它残留；`data-hello-line` 的两处引用（创建标记 + renderSession 显隐）均服务于 rail-greeting，逻辑自洽。
