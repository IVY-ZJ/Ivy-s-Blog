# robots.txt — crawler guidance (honor-based: blocks polite bots only;
# for real abuse protection put the site behind a CDN/WAF, e.g. Cloudflare).
User-agent: *
Allow: /

# Keep internal JS/CSS out of search indexes (optional, harmless).
Disallow: /assets/js/
Disallow: /assets/css/

# Course files are served through the viewer (obfuscated .dat, runtime
# decryption + login guard); do not let crawlers index the raw assets.
Disallow: /assets/files/
